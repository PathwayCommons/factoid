// import { expect } from 'chai';
// import { COLLECTIONS } from '../../src/util/registry';
// import hint from '../../src/server/routes/api/document/hint';

// const { dbPrefix, dbName } = COLLECTIONS.PUBMED;

// describe('hints', function(){
//   describe('find', function(){

//     const publicationXref = { dbPrefix, dbName, id: null };

//     it('Should return empty when no publication Xref provided', async function(){
//       const { organismOrdering, entities } = await hint.find( publicationXref );
//       expect( organismOrdering ).to.be.empty;
//       expect( entities ).to.be.empty;
//     });

//   }); // find
// }); // hints
